# 企微 Webhook 消息推送（Markdown 版本）

> 官方文档：https://developer.work.weixin.qq.com/document/path/99110

---

## 接口信息

- **请求方式**：POST（HTTPS）
- **请求地址**：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的KEY`
- **Content-Type**：`application/json`
- **频率限制**：每个 webhook 最多 **20 条/分钟**

---

## Markdown 消息格式

```json
{
    "msgtype": "markdown",
    "markdown": {
        "content": "你的 markdown 内容"
    }
}
```

- `content` 最长不超过 **4096 字节**，必须 UTF-8 编码
- 支持 `<@userid>` 语法 @ 群成员（会触发提醒）

---

## 支持的 Markdown 语法

| 语法 | 写法 |
|------|------|
| 标题（1-6级） | `# 标题`（# 与文字间要有空格） |
| 加粗 | `**bold**` |
| 链接 | `[文字](url)` |
| 行内代码 | `` `code` ``（不支持跨行） |
| 引用 | `> 引用文字` |
| 字体颜色 | `<font color="info">绿色</font>` |

### 颜色只支持 3 种

| color 值 | 颜色 |
|----------|------|
| `info` | 绿色 |
| `comment` | 灰色 |
| `warning` | 橙红色 |

---

## @ 群成员

在 `content` 中使用 `<@userid>` 即可 @ 对应的群成员：

```json
{
    "msgtype": "markdown",
    "markdown": {
        "content": "请 <@zhangsan> 查看今日更新\n> 新增数据 <font color=\"warning\">15 条</font>"
    }
}
```

> 注意：`userid` 是企微后台的成员 ID，不是姓名。

---

## Python 示例

```python
import requests

WEBHOOK_URL = "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=你的KEY"


def send_markdown(content: str):
    """发送 markdown 消息到企微群。"""
    payload = {
        "msgtype": "markdown",
        "markdown": {
            "content": content
        }
    }
    resp = requests.post(WEBHOOK_URL, json=payload, timeout=10)
    result = resp.json()
    if result.get("errcode") != 0:
        print(f"发送失败: {result}")
    return result


# ===== 示例 =====

# 简单文本
send_markdown("## 今日数据更新\n> 新增 **15** 条资讯")

# 带颜色
send_markdown(
    "## 爬虫执行完成\n"
    "- 新增: <font color=\"info\">12 条</font>\n"
    "- 跳过: <font color=\"comment\">88 条</font>\n"
    "- 失败: <font color=\"warning\">0 条</font>"
)

# @ 某人
send_markdown(
    "## 重点事项提醒\n"
    "以下事项即将到期，请 <@zhangsan> <@lisi> 关注：\n"
    "- DNF 5月25日更新公告\n"
    "- 王者荣耀 5月26日新赛季"
)

# @ 所有人（text 类型才支持 @all，markdown 不支持 @all）
```

---

## 注意事项

1. **保护好 webhook 地址**，不要提交到 Git 或公开分享
2. Markdown 不支持 `@all`，如需 @所有人 请用 text 类型
3. Markdown 不支持 `mentioned_list` / `mentioned_mobile_list` 参数
4. 换行用 `\n`
5. 项目中 webhook 地址配置在 `.env` 的 `WECOM_WEBHOOK` 变量
